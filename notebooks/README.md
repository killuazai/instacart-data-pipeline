# Databricks Notebooks

Optional folder for exported `.ipynb` notebooks if the team uses notebook versions of the pipeline or presentation/demo notebooks.

Keep the source SQL in `src/` as the clearest layer-by-layer implementation when possible.

test
